#pragma once


#include <limits>
#include "Particle.h"

namespace pso {

	// Rastrigin function, search domain [-5.12, 5.12], global min f(0) = 0
	
	//double Rastrigin(const Particle& particle) {
	
	//}
	
	// Sphere function, search domain [-inf; inf], global min f(0) = 0
}